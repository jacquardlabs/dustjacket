[project]
name = "httpcache"
version = "0.3.1"
description = "An HTTP response cache for Python with memory and disk backends."
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = []

[project.urls]
Homepage = "https://github.com/example/httpcache"
